import React from "react";

function MainTextProduct({ title }) {
    return (
        <header className="text-center mt-10">
            <h1 className="text-5xl font-bold capitalize">{title}</h1>
        </header>
    );
}

export default MainTextProduct;
